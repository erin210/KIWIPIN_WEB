{
  "name": "kiwipin_web",
  "version": "1.0.0",
  "description": "",
  "main": "config.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass-watch": "sass --watch scss:assets/css --embed-source-map --embed-sources",
    "start": "node server.js",
    "dev": "concurrently \"npm run sass-watch\" \"npm start\"",
    "build": "sass scss:assets/css --embed-source-map --embed-sources --style=compressed"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "ejs": "^3.1.10",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "sass": "^1.89.2"
  }
}